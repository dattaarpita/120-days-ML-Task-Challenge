Task:Call len() on a list of 1 billion items

Explantion:Here a list of 1 billion numbers defined.Then .len()measured. Here .len() didnt count the number manually.Python has a internal counter named
"ob_size". It stored the length. .len() only gives the output of "ob_size"
