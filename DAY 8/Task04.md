Task: Compare list.pop() vs list.pop(0)

Explanation:
Here .pop() used that removes last element from the list and .pop(0) removes first element from the list. all existing numbers move one step backward.
