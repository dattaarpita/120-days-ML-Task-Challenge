Task01:  Create a list of 10 million numbers. Check if -5 is in the list.

Explanation:
In this task function named "linear_search" is defined passing two parameters.A list is created ranges of 10 million numbers.Target variable is -5. Then for loop run through this list. If current number matches the target number then print "Number Found".Otherwise print "Number not Found"
In this linear search ,it cheacks all the 10 million numbers one by one. It takes much time.Time complexity O(N).


