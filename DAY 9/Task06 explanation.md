In this code, 2 generators formed. 1st generator will create the square of the 10 numbers(0 to 9) as range is given 10.
Second generator will filter the even numbers from those squared numbers.Then print those number using next(). when generator exhausted except block will run
