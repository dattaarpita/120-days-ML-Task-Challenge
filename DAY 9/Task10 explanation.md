In this code,firstly next() executes as there are no values it will print else block.
Then after sending first value,it will go left side's variable of the yield. 
then exceutes next 2 lines then again goes to yield to execute the operation and pauses again until next value finds.
Same operation repeats when generator sends value.Generator created for calculating average number.
