The original function takes two arguments and do the summation.The wrapper function accepts those arguments using *args and **kwargs.
func(*args, **kwargs) calls old_func(2, 3),erxecute the dum, return result 5, then print shis the result
