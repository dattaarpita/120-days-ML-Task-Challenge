By using @wrapper, the original function does not need to be manually assigned to the wrapper.The decorator automatically passes the original function to wrapper.
when the original function is called, the wrapped function runs with its extra code and original code.
